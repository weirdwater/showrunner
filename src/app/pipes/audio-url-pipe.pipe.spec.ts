import { AudioUrlPipePipe } from './audio-url-pipe.pipe';

describe('AudioUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AudioUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
