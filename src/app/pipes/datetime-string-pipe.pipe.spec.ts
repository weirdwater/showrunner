import { DatetimeStringPipePipe } from './datetime-string-pipe.pipe';

describe('DatetimeStringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeStringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
